
		$(document).ready(function() {
  		if ($("a#instructionsr").length) {
  		  console.log('got it!')
  			$("a#instructionsr").fancybox({
  				'titlePosition'	: 'over'
  			});
  		}
		});
